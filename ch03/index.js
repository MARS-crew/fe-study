/**
 * a, b 두 개의 숫자를 받아서 더한 값을 반환하는 함수
 * @param {Number} a
 * @param {Number} b
 * @returns {Number}
 */
function add(a, b) {
  return a + b;
}

const response = add(1, 2);
console.log(response + 3);
